apiVersion: apps/v1
kind: Deployment
metadata:
  name: modoki-operator
spec:
  replicas: 1
  selector:
    matchLabels:
      name: modoki-operator
  template:
    metadata:
      labels:
        name: modoki-operator
    spec:
      serviceAccountName: modoki-operator
      containers:
        - name: modoki-operator
          # Replace this with the built image name
          image: REPLACE_IMAGE
          command:
          - modoki-operator
          imagePullPolicy: Always
          env:
            - name: WATCH_NAMESPACE
              valueFrom:
                fieldRef:
                  fieldPath: metadata.namespace
            - name: POD_NAME
              valueFrom:
                fieldRef:
                  fieldPath: metadata.name
            - name: OPERATOR_NAME
              value: "modoki-operator"
